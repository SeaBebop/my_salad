import React from 'react';
import classes from './OrderHeader.module.css';

const OrderHeader = (props) => {
  return (
    <header className={classes.header}>
      <h1>Discover Your Salad Adventure</h1>
    </header>
  );
};

export default OrderHeader;
